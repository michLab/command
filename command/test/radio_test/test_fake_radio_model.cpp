#include <gtest/gtest.h>

// #include "radio/fake_radio_model.h"

TEST(TestFakeRadioModel, UsePowerPreconditions_SetValidPower_Works)
{
  // radio::FakeRadioModel::Limits limits;
  // limits.min_power = 0;
  // limits.max_power = 10;
  // limits.min_freq = 200;
  // limits.max_freq = 400;

  // radio::FakeRadioModel radio(limits);

  // radio::Power power_target = (limits.max_power - limits.min_power) / 2;

  // try
  // {
  //   radio.set_power(power_target);
  // }
  // catch (const std::runtime_error& exc)
  // {
  // }
  // std::cout << "Finishing test...\n";
  EXPECT_TRUE(true);
}